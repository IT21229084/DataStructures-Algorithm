 A = []
  for v in range(1,5):
     A.append(int(input("Enter Values: ")))
  
  print(A)
   


 def BubbleSort(A)
   n = len(A)
   
   for i in Range(1,n):
     for J in Range(1,n-i+1):
       if(A[j] < A[j-1]:
         A[j],A[j-1] = A[j-1],A[j]

BubbleSort(A)
print(A)